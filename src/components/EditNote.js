import React from 'react'

const EditNote = ()=>{
    return (
        <div>
            <form>
                <input id='title'/>
                <button onClick={()=>{}}>Save Changes</button>
            </form>
        </div>
    )
}

export default EditNote